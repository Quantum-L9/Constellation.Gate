global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/gate.rules.yml

scrape_configs:
  - job_name: constellation-gate
    metrics_path: /metrics
    static_configs:
      - targets:
          - gate:9000
