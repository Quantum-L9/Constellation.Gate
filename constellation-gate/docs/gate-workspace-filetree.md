# constellation-gate — single package root

The **constellation-gate** repository (everything described in the target tree below) lives in **one folder** at the workspace root:

| Path | Role |
|------|------|
| `constellation-gate/` | Canonical tree (`src/`, `tests/`, packaging, CI metadata) |

**Full target layout (ASCII tree + naming notes):** `target-filetree.md` (same directory as this file).
